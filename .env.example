# Skill-0 Environment Configuration
# Copy to .env and customize values

# === Core API ===
SKILL0_DB_PATH=skills.db
SKILL0_PARSED_DIR=parsed
SKILL0_ENV=development

# === Governance ===
SKILL0_GOVERNANCE_DB_PATH=governance/db/governance.db
SKILL0_TOOLS_PATH=tools

# === GPU / Device ===
# Options: auto, cpu, cuda
SKILL0_DEVICE=auto

# === CORS ===
# Comma-separated allowed origins
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# === Authentication (JWT) ===
# IMPORTANT: Change JWT_SECRET_KEY in production!
JWT_SECRET_KEY=dev-secret-change-in-production
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=30
# API login credentials (required in production)
API_USERNAME=admin
API_PASSWORD=change-me

# === Rate Limiting ===
# Format: {count}/{period} — e.g. 100/minute, 1000/hour
API_RATE_LIMIT=100/minute
AUTH_RATE_LIMIT=10/minute

# === Logging ===
# Options: DEBUG, INFO, WARNING, ERROR
SKILL0_LOG_LEVEL=INFO
SKILL0_LOG_FORMAT=console
