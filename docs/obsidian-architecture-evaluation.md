# Obsidian 資料庫架構研究與 Skill-0 適用性評估

**作者**: pingqLIN  
**日期**: 2026-02-08  
**版本**: 1.0.0

## 執行摘要

本文檔研究 Obsidian 知識管理軟體的資料庫建制方式，聚焦於**內部連結（Internal Links）** 與**關係祖譜（Graph View / Relationship Genealogy）** 兩種核心架構，並評估其概念與機制對 Skill-0 專案的可吸收與參考之處。

研究結論：Obsidian 的三項核心機制——**雙向連結**、**Metadata Cache**、**知識圖譜視覺化**——對 Skill-0 的技能間關係建模與知識發現具有直接參考價值，建議分階段導入。

---

## 目錄

1. [Obsidian 架構概述](#1-obsidian-架構概述)
2. [內部連結機制](#2-內部連結機制)
3. [關係祖譜與圖譜視圖](#3-關係祖譜與圖譜視圖)
4. [Properties 與 Metadata 系統](#4-properties-與-metadata-系統)
5. [Map of Content (MOC) 模式](#5-map-of-content-moc-模式)
6. [Skill-0 現狀分析](#6-skill-0-現狀分析)
7. [適用性評估與建議](#7-適用性評估與建議)
8. [具體導入方案](#8-具體導入方案)
9. [結論](#9-結論)
10. [參考資料](#10-參考資料)

---

## 1. Obsidian 架構概述

### 1.1 核心設計理念

Obsidian 是一款基於**本地 Markdown 檔案**的個人知識管理系統（PKM），核心理念為：

- **Local-First**: 所有筆記為純 Markdown 文件，存於本地資料夾（稱為 Vault）
- **Linked Thought**: 以連結（而非資料夾）作為知識組織的主要方式
- **Graph-Based Discovery**: 透過圖譜揭示隱含關係與模式

### 1.2 技術架構層次

```
┌─────────────────────────────────────────────────────┐
│                 視覺化層 (Graph View)                 │
│          力導向圖 / 互動式節點-邊關係圖                │
├─────────────────────────────────────────────────────┤
│              查詢與索引層 (Dataview / Search)          │
│       結構化查詢 / 動態列表 / 類 SQL 語法               │
├─────────────────────────────────────────────────────┤
│              Metadata Cache 層                       │
│     MetadataCache 類別 / resolvedLinks / CachedMetadata │
├─────────────────────────────────────────────────────┤
│              連結解析層 (Link Resolution)              │
│     Wikilink / Backlink / Block Reference / Alias     │
├─────────────────────────────────────────────────────┤
│              儲存層 (Vault / Filesystem)               │
│          純 Markdown 檔案 + YAML Frontmatter          │
└─────────────────────────────────────────────────────┘
```

---

## 2. 內部連結機制

### 2.1 連結類型

Obsidian 支援多種內部連結語法：

| 連結類型 | 語法範例 | 說明 |
|---------|---------|------|
| **Wikilink** | `[[筆記名稱]]` | 最常用，自動追蹤連結 |
| **Markdown Link** | `[顯示文字](筆記名稱.md)` | 標準 Markdown 語法 |
| **標題連結** | `[[筆記名稱#標題]]` | 連結到特定標題 |
| **區塊連結** | `[[筆記名稱#^block-id]]` | 連結到特定段落區塊 |
| **別名連結** | `[[筆記名稱|顯示名稱]]` | 自訂顯示文字 |

### 2.2 雙向連結（Bidirectional Links）

Obsidian 的核心創新在於**自動雙向連結**：

- 當筆記 A 連結到筆記 B 時，B 自動顯示 A 的**反向連結（Backlink）**
- 系統同時追蹤**已連結提及**（Linked Mentions）和**未連結提及**（Unlinked Mentions）
- 這種對稱性鼓勵網狀知識結構，而非嚴格的層級結構

```
    筆記 A ────連結到────→ 筆記 B
       ←────反向連結────
       
    (自動建立雙向關係，無需手動維護)
```

### 2.3 連結維護機制

- **自動更新**: 重新命名或移動筆記時，所有內部連結自動更新
- **別名解析**: YAML Frontmatter 中定義的 aliases 在連結建議中自動解析
- **孤立偵測**: 圖譜中無連結的筆記（Orphan Notes）會被標記

### 2.4 Skill-0 啟示

> **關鍵洞察**: Obsidian 的雙向連結證明了「關係是第一等公民」的設計理念。Skill-0 目前僅支援技能*內部*的元素關係（Action ↔ Rule ↔ Directive），但缺乏技能*之間*的雙向連結機制。

---

## 3. 關係祖譜與圖譜視圖

### 3.1 圖譜視圖（Graph View）

Obsidian 的圖譜視圖將知識庫轉化為互動式力導向圖：

- **節點（Nodes）**: 每個 Markdown 檔案為一個節點，連結越多節點越大
- **邊（Edges）**: 內部連結構成節點間的邊，可為雙向
- **群集（Clusters）**: 高度互連的筆記自然形成群集，揭示知識主題

### 3.2 全域圖譜 vs 局部圖譜

| 特性 | 全域圖譜 (Global Graph) | 局部圖譜 (Local Graph) |
|------|------------------------|----------------------|
| **範圍** | Vault 中所有筆記 | 以當前筆記為中心 |
| **深度** | 完整網路 | 可設定連結深度（1度、2度...） |
| **用途** | 發現全域模式與群集 | 探索相關脈絡 |
| **篩選** | 可依資料夾/標籤/搜尋篩選 | 自動聚焦相關筆記 |

### 3.3 關係祖譜類比

在 Zettelkasten 方法論中，筆記關係類似家族祖譜：

```
                    ┌──────────┐
                    │  祖先筆記   │  (原始概念 / 來源)
                    │ Ancestor  │
                    └─────┬────┘
                          │
              ┌───────────┼───────────┐
              │           │           │
        ┌─────┴────┐ ┌───┴────┐ ┌────┴─────┐
        │  後代筆記1  │ │ 後代筆記2 │ │  後代筆記3  │
        │ Child 1   │ │ Child 2 │ │ Child 3   │
        └─────┬────┘ └────────┘ └──────────┘
              │
        ┌─────┴────┐
        │  孫代筆記   │  (更細化的概念)
        │ Grandchild│
        └──────────┘
```

但 Obsidian 的圖譜**突破了嚴格的樹狀結構**：
- 一個筆記可以有多個「父輩」（多重來源影響）
- 關係是**網狀**而非樹狀
- 透過 Backlinks 和 Tags 自然形成群集

### 3.4 Metadata Cache 內部結構

Obsidian 使用 `MetadataCache` 類別進行高效索引：

```typescript
// Obsidian 內部資料結構 (概念示意)
interface MetadataCache {
  resolvedLinks: Record<string, Record<string, number>>   // 已解析連結
  unresolvedLinks: Record<string, Record<string, number>> // 未解析連結
  getFileCache(file: TFile): CachedMetadata               // 取得檔案快取

  // 事件系統
  on('changed', callback)    // 檔案變更通知
  on('resolved', callback)   // 連結解析完成
  on('deleted', callback)    // 檔案刪除
}

interface CachedMetadata {
  links: LinkCache[]          // 出站連結
  embeds: EmbedCache[]        // 嵌入內容
  tags: TagCache[]            // 標籤
  headings: HeadingCache[]    // 標題
  frontmatter: FrontMatter    // YAML 元資料
  sections: SectionCache[]    // 章節
  blocks: Record<string, BlockCache>  // 區塊參考
}
```

**效能特性**:
- 記憶體內快取，避免重複檔案讀取
- 事件驅動的即時更新
- 支援大型 Vault（10,000+ 筆記）的最佳化策略

---

## 4. Properties 與 Metadata 系統

### 4.1 YAML Frontmatter 即資料庫欄位

Obsidian 將 YAML Frontmatter 視為結構化的 Metadata：

```yaml
---
title: 機器學習基礎
created: 2026-02-08
tags: [AI, 機器學習, 入門]
status: published
related:
  - [[深度學習]]
  - [[神經網路]]
aliases:
  - ML Basics
  - 機器學習入門
---
```

### 4.2 資料庫類比

| 資料庫概念 | Obsidian 對應 |
|-----------|-------------|
| **Table** | 資料夾 / 查詢結果集 |
| **Column / Field** | YAML Frontmatter 中的 Property |
| **Row / Record** | 單一 Markdown 筆記 |
| **Data Types** | Text, Number, Date, List, Boolean, Link |
| **Query** | Search / Dataview / Bases 插件 |
| **Index** | Metadata Cache |

### 4.3 Dataview 查詢

Dataview 插件將 Vault 視為可查詢的資料庫：

```dataview
TABLE title, status, tags
FROM "Projects"
WHERE status = "active"
SORT created DESC
```

---

## 5. Map of Content (MOC) 模式

### 5.1 MOC 概念

Map of Content 是 Obsidian 社群發展出的組織模式，類似「知識索引頁」：

- **功能**: 作為特定主題的導航中心（Hub）
- **結合手動與動態**: 手動精選重要連結 + Dataview 自動生成列表
- **階層靈活**: 可建立多層級 MOC（主題 MOC → 子主題 MOC → 筆記）

### 5.2 MOC 與 Skill-0 的類比

```
Obsidian MOC                          Skill-0 類比
─────────────────────────────────────────────────────
主題 MOC                    →          技能類別 (category)
子主題 MOC                  →          技能群組 / 技能家族
Wikilink [[筆記]]           →          skill_id 參照
Backlink（自動反向連結）      →          技能間雙向依賴
Dataview Query              →          語義搜尋 API
Tag (#標籤)                 →          技能標籤 / action_type
Property (Frontmatter)      →          schema meta 欄位
```

---

## 6. Skill-0 現狀分析

### 6.1 目前的關係模型

Skill-0 目前支援的關係類型：

| 層級 | 關係類型 | 說明 | 對應欄位 |
|------|---------|------|---------|
| **技能內部** | Action → Rule | 執行路徑中的依賴 | `execution_paths.sequence` |
| **技能內部** | Directive → Action/Rule | 指令關聯元素 | `related_elements` |
| **技能內部** | Rule → 分支目標 | 規則失敗時的分支 | `branching_targets` |
| **技能間** | ❌ 不存在 | 技能間無連結機制 | — |

### 6.2 現有基礎設施

```
✅ 已有                          ❌ 缺少
──────────────────────────────────────────────────
SQLite 向量資料庫               技能間關係表 (skill_links)
語義搜尋 (similarity)           雙向連結 (backlinks)
技能元資料表 (skills)            圖譜視覺化 (graph view)
JSON Schema 2.2.0              MOC 索引機制
Embeddings (384-dim)            關係類型分類 (link_type)
```

### 6.3 GAP 分析

| Obsidian 機制 | Skill-0 現狀 | 差距 |
|--------------|-------------|------|
| 雙向連結 (Backlinks) | 無技能間連結 | **高** |
| Graph View | 無圖譜視覺化 | **高** |
| Metadata Cache | 有 SQLite 但無連結快取 | **中** |
| Properties (YAML) | 有 JSON Schema 元資料 | **低** |
| Dataview 查詢 | 有語義搜尋 API | **低** |
| MOC 導航 | 無技能索引頁 | **中** |
| Alias 解析 | 無別名系統 | **低** |

---

## 7. 適用性評估與建議

### 7.1 高價值借鑑項目

#### 🔗 借鑑一：技能間雙向連結（Inter-Skill Bidirectional Links）

**Obsidian 概念**: 筆記間的自動雙向連結與反向連結  
**Skill-0 應用**: 建立技能間的顯式關係，支援雙向查詢

**價值**:
- 發現技能間的依賴與組合關係
- 支援「相關技能推薦」功能
- 建立技能生態的網狀結構

**建議方案**:
```json
{
  "skill_links": [
    {
      "target_skill_id": "claude__pdf_extraction",
      "link_type": "depends_on",
      "description": "需要 PDF 提取功能作為前置步驟",
      "bidirectional": true
    },
    {
      "target_skill_id": "mcp__filesystem__read",
      "link_type": "uses",
      "description": "使用檔案系統讀取工具"
    }
  ]
}
```

#### 📊 借鑑二：技能關係圖譜（Skill Graph View）

**Obsidian 概念**: 力導向圖視覺化所有筆記間的關係  
**Skill-0 應用**: 在 Dashboard 中增加技能關係圖譜視圖

**價值**:
- 視覺化技能生態全景
- 發現技能群集與孤立技能
- 輔助技能組合決策

#### 🗂️ 借鑑三：技能 Map of Content（Skill MOC）

**Obsidian 概念**: 主題導航中心，結合手動精選與動態查詢  
**Skill-0 應用**: 自動生成技能分類索引頁，結合語義搜尋

### 7.2 中等價值借鑑項目

| 借鑑項目 | 說明 | 複雜度 |
|---------|------|--------|
| **連結類型分類** | 借鑑 Obsidian 的多種連結語法，定義技能間關係類型 | 低 |
| **Metadata Cache** | 建立技能關係的記憶體快取，加速圖譜查詢 | 中 |
| **別名系統** | 為技能增加別名，提升搜尋命中率 | 低 |

### 7.3 低價值 / 不適用項目

| 項目 | 不適用原因 |
|------|-----------|
| **Wikilink 語法** | Skill-0 使用 JSON Schema，非 Markdown |
| **Vault 檔案系統** | Skill-0 已有 SQLite 資料庫，無需純檔案架構 |
| **Zettelkasten 原子筆記** | Skill-0 已有三元分類系統（Action/Rule/Directive） |

---

## 8. 具體導入方案

### 8.1 Phase 1：技能間連結模型（Schema 擴展）

在 JSON Schema 中新增 `skill_links` 欄位：

```json
{
  "skill_links": {
    "type": "array",
    "description": "技能間連結關係（借鑑 Obsidian 雙向連結概念）",
    "items": {
      "type": "object",
      "required": ["target_skill_id", "link_type"],
      "properties": {
        "target_skill_id": {
          "type": "string",
          "description": "目標技能識別碼"
        },
        "link_type": {
          "type": "string",
          "enum": [
            "depends_on",
            "extends",
            "composes_with",
            "alternative_to",
            "related_to",
            "derived_from",
            "parent_of"
          ],
          "description": "關係類型"
        },
        "description": {
          "type": "string",
          "description": "關係說明"
        },
        "strength": {
          "type": "number",
          "minimum": 0,
          "maximum": 1,
          "description": "關係強度（0=弱關聯, 1=強依賴）"
        },
        "bidirectional": {
          "type": "boolean",
          "default": false,
          "description": "是否為雙向關係（自動建立反向連結）"
        }
      }
    }
  }
}
```

**連結類型定義**:

| link_type | 說明 | 類比 |
|-----------|------|------|
| `depends_on` | 執行時依賴 | Obsidian `[[prerequisite]]` |
| `extends` | 功能擴展 | 子類別關係 |
| `composes_with` | 可組合使用 | 協作關係 |
| `alternative_to` | 替代方案 | 同功能不同實作 |
| `related_to` | 主題相關 | Obsidian Backlink |
| `derived_from` | 衍生自 | 來源追溯 |
| `parent_of` | 父技能 | 祖譜中的上級節點 |

### 8.2 Phase 2：雙向連結索引（資料庫擴展）

在 SQLite 中新增技能連結表：

```sql
-- 技能連結表（借鑑 Obsidian resolvedLinks）
CREATE TABLE IF NOT EXISTS skill_links (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    source_skill_id TEXT NOT NULL,
    target_skill_id TEXT NOT NULL,
    link_type TEXT NOT NULL,
    description TEXT,
    strength REAL DEFAULT 0.5,
    bidirectional BOOLEAN DEFAULT 0,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    UNIQUE(source_skill_id, target_skill_id, link_type)
);

-- 反向連結索引（借鑑 Obsidian Backlinks）
CREATE INDEX idx_skill_links_target ON skill_links(target_skill_id);
CREATE INDEX idx_skill_links_source ON skill_links(source_skill_id);
CREATE INDEX idx_skill_links_type ON skill_links(link_type);

-- 反向連結查詢視圖
CREATE VIEW IF NOT EXISTS skill_backlinks AS
SELECT 
    target_skill_id AS skill_id,
    source_skill_id AS linked_from,
    link_type,
    description,
    strength
FROM skill_links
UNION ALL
SELECT 
    source_skill_id AS skill_id,
    target_skill_id AS linked_from,
    -- 反轉關係類型
    CASE link_type
        WHEN 'depends_on' THEN 'depended_by'
        WHEN 'extends' THEN 'extended_by'
        WHEN 'parent_of' THEN 'child_of'
        ELSE link_type
    END AS link_type,
    description,
    strength
FROM skill_links
WHERE bidirectional = 1;
```

### 8.3 Phase 3：關係圖譜視覺化（Dashboard 擴展）

在 Skill-0 Dashboard 中增加圖譜視圖：

```
┌──────────────────────────────────────────────────┐
│                Skill-0 Dashboard                  │
├──────────────────────────────────────────────────┤
│  ┌──────────┐  ┌──────────┐  ┌──────────┐       │
│  │ 技能列表  │  │ 語義搜尋  │  │ 關係圖譜  │  ← 新增 │
│  └──────────┘  └──────────┘  └──────────┘       │
│                                                  │
│     [PDF提取]──depends_on──→[檔案讀取]            │
│        │                       │                 │
│        │ extends               │ composes_with   │
│        ↓                       ↓                 │
│     [表格分析]              [資料轉換]            │
│        │                                         │
│        └────related_to────→[Excel匯出]           │
│                                                  │
│  節點大小 = 連結數量（借鑑 Obsidian Graph View）    │
│  群集顏色 = 技能類別                              │
└──────────────────────────────────────────────────┘
```

### 8.4 實作優先序與時間估算

| 階段 | 工作項目 | 優先級 | 預估工時 |
|------|---------|--------|---------|
| Phase 1 | Schema 新增 `skill_links` 欄位 | P0 | 2-4 小時 |
| Phase 1 | 定義 7 種連結類型 | P0 | 1 小時 |
| Phase 2 | SQLite 新增 `skill_links` 表 | P1 | 4-6 小時 |
| Phase 2 | 實作反向連結查詢 API | P1 | 4-6 小時 |
| Phase 2 | 技能索引時自動建立連結 | P1 | 6-8 小時 |
| Phase 3 | Dashboard 圖譜視圖（前端） | P2 | 16-24 小時 |
| Phase 3 | 圖譜資料 API 端點 | P2 | 4-6 小時 |

---

## 9. 結論

### 9.1 核心價值摘要

| Obsidian 機制 | Skill-0 借鑑價值 | 推薦指數 |
|--------------|-----------------|---------|
| **雙向連結** | 建立技能間關係網路 | ⭐⭐⭐⭐⭐ |
| **Graph View** | 視覺化技能生態 | ⭐⭐⭐⭐⭐ |
| **Metadata Cache** | 加速關係查詢 | ⭐⭐⭐⭐ |
| **MOC 模式** | 技能分類索引 | ⭐⭐⭐⭐ |
| **Dataview 查詢** | 結構化搜尋（已有語義搜尋） | ⭐⭐⭐ |
| **Properties** | 元資料擴展（已有 Schema） | ⭐⭐⭐ |
| **Alias 系統** | 別名搜尋 | ⭐⭐ |

### 9.2 總結

Obsidian 的設計哲學——**「連結優先於分類」**——對 Skill-0 具有重要啟示。目前 Skill-0 在技能*內部*的結構化分解已相當成熟（Action/Rule/Directive 三元分類），但在技能*之間*的關係建模仍有顯著空間。

借鑑 Obsidian 的雙向連結與圖譜視覺化機制，Skill-0 可以：

1. **建立技能關係網路**：從孤立的技能集合升級為互聯的技能生態
2. **支援智慧推薦**：基於連結關係推薦相關或互補技能
3. **視覺化技能圖譜**：在 Dashboard 中提供類似 Obsidian Graph View 的體驗
4. **增強可追溯性**：透過 `derived_from` 和 `parent_of` 關係追溯技能演化

**終極願景**: 將 Skill-0 從「技能分解工具」升級為「技能知識圖譜平台」，借鑑 Obsidian 的 Linked Thought 理念，實現技能的網狀組織與智慧發現。

---

## 10. 參考資料

### Obsidian 官方文件
- **Internal Links**: https://help.obsidian.md/links
- **Graph View**: https://help.obsidian.md/plugins/graph
- **Backlinks**: https://help.obsidian.md/Plugins/Backlinks
- **Properties**: https://help.obsidian.md/properties

### 技術分析
- **Internal Links and Graph View (DeepWiki)**: https://deepwiki.com/obsidianmd/obsidian-help/4.2-internal-links-and-graph-view
- **Graph View Architecture (DeepWiki)**: https://deepwiki.com/obsidianmd/obsidian-help/4.5-graph-view
- **Metadata and Linking (Plugin Docs)**: https://deepwiki.com/obsidianmd/obsidian-plugin-docs/3.5-metadata-and-linking
- **Properties and Metadata (DeepWiki)**: https://deepwiki.com/obsidianmd/obsidian-help/4.3-properties-and-metadata
- **Graph View Source (GitHub)**: https://github.com/obsidianmd/obsidian-help/blob/master/en/Plugins/Graph%20view.md

### 社群資源
- **Obsidian Zettelkasten Starter Kit**: https://github.com/groepl/Obsidian-Zettelkasten-Starter-Kit
- **Obsidian MOC Guide**: https://github.com/seqis/ObsidianMOC
- **Dataview Plugin**: https://blacksmithgu.github.io/obsidian-dataview/
- **Backlink Cache Plugin**: https://github.com/mnaoumov/obsidian-backlink-cache

### Skill-0 相關
- **Skill-0 Repository**: https://github.com/pingqLIN/skill-0
- **Schema**: `schema/skill-decomposition.schema.json` v2.2.0
- **Vector Store**: `src/vector_db/vector_store.py`
- **Semantic Search**: `src/vector_db/search.py`

---

**文件版本**: 1.0.0  
**最後更新**: 2026-02-08  
**作者**: pingqLIN  
