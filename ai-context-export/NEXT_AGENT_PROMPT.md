# Next Agent Prompt

Read `PROJECT_SUMMARY.md`, `CURRENT_STATE.md`, `TODO.md`, then `../docs/README.md` before acting.

The repository is in Runtime v4 closeout under feature freeze. Preserve dry-run-only behavior, single-host Docker Compose, and the three SQLite stores. Do not add a subsystem, service, database, framework, dependency, production adapter, schema feature, parser feature, or UI redesign.

Execute only the remaining item in `TODO.md`. Use existing doctor, backup/restore, healthcheck, rehearsal, and test paths. Record every acceptance result with command, exit code, and artifact. If a new production-source change is required, or any stop condition is reached, create `SAFETY_STOP.md` instead of expanding scope.
